name: magiil_mart
description: "Magiil Mart - Flutter Supermarket App"

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  # State management
  provider: ^6.1.2

  # Supabase (Auth + Database)
  supabase_flutter: ^2.8.1

  # File & Excel handling
  file_picker: ^8.0.0
  excel: ^4.0.2

  # Cart persistence
  shared_preferences: ^2.2.0

  # Image picker
  image_picker: ^1.0.0

  # Icons
  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true

  # ✅ ASSETS (THIS WAS MISSING)
  assets:
    - assets/images/banners/
    - assets/images/categories/
